# 优化 & 重构指令

> 本文件指导 AI 助手根据 `ISSUES.md` 中的问题清单逐步优化和重构项目代码。
> 每次只处理一个 ISSUE,完成后更新 `ISSUES.md` 状态。

---

## 入口

1. 读取 `ISSUES.md`,获取全部问题列表
2. 按优先级顺序处理:**P0 → P1 → P2 → P3**,同优先级内按编号顺序

---

## 处理流程(每个 ISSUE)

1. **阅读**:打开 ISSUE 中标注的文件和行号,理解现状
2. **分析**:确认修改范围、影响面、是否有牵连的其他文件,选择最合适、最优的方式处理
3. **修改**:写代码(必要时先备份/加注释)
4. **验证**:确保 `python -m py_compile` 所有涉及文件编译通过,不报错即可;不需要跑实际 Excel 流程验证
5. **更新 ISSUES.md**:将该问题标记为 `[已修复]`,补一句修复说明

---

## 重构原则

1. **单 ISSUE 单 commit**:一个问题修完再修下一个,不混改
2. **保持向后兼容**:外部接口不变(pipeline 入口、config 字段名不变;deprecated 的东西才删)
3. **先修 P0/P1,不跳过**:地基不稳不碰上层
4. **每次改完验证**:`py_compile` 所有涉及文件,不报错即可;无需跑实际 xlsx 流程
5. **ISSUES.md 同步更新**:改完立刻标 `[已修复]`,补简要说明

---

## 不动的部分

- `tools/` 下非核心工具程序暂不重构(如 `hotwords.py`、`title_optimize/`),除非关联 ISSUE 明确要求
- `fashion_filter/` 下程序除非 ISSUE 涉及,否则不动
- 项目目录结构保持不变
- `README.md`、`ISSUES.md`、本文件自身保持同步但不在 ISSUE 范围内
- **未启用/未完成的代码不删除**:如 `RemoveEmptyJStep`(注释掉)、TODO 标记等,保留原状(注释或占位),后续可能启用或补充
